import styled from 'styled-components';

const StyledDashboard = styled.div`
    margin-top: 72px;

    main > div{  
        margin-top: 20px;
        gap: 20px;
    }

`;

export default StyledDashboard;