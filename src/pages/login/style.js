import styled from 'styled-components';

const StyledLoginPage = styled.div`
    min-height: 100vh;
    display: flex;
    align-items: center;

    >div{
        gap: 20px;
    }
`;

export default StyledLoginPage;